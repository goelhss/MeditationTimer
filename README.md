# Meditation Timer

A calm Android phone timer for meditation with configurable interval dings, reliable screen-locked operation, session logs, and text-file sharing.

## Build

```sh
UPLOAD_PLAY=0 scripts/build.sh
```

See `scripts/build_steps.md` and `Testing.md` for delivery and test-suite details.
